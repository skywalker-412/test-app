apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "mysql-quizdb.fullname" . }}-pvc
spec:
  accessModes:
    - {{ .Values.persistence.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
